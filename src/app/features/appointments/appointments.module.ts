/// <reference path="appointments.routing.ts" />
import { NgModule } from "@angular/core";
import { routing } from "./appointments.routing";

@NgModule({
    declarations: [

    ],
    imports: [
        routing,

    ]
})
export class AppointmentsModule {
}
